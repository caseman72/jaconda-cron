#!/usr/bin/env ruby
#
# Stub executable for jaconda-jira-cron
#

require File.expand_path('../../config/environment',  __FILE__)

DaemonKit::Application.exec( DAEMON_ROOT + '/libexec/jaconda-jira-cron-daemon.rb' )
